# Design: UC-000 — <UC name>

**UC:** `specs/<core|craft>/br-###/use-cases/UC-000-<slug>/UC-000.md` ← the real spec lives there; this file is only *how it gets built*
**Gate:** `.sdd/gate/UC-000.ok` · **Date:** ___ · **Status:** draft | agreed

## Summary
<Three to five sentences: what this UC builds, and by which route. Write it for the person reading six months from now.>

## Technical context
- **Language / runtime:** ___
- **Main dependencies:** ___
- **Storage:** ___ (where the SOURCE OF TRUTH of the data lives — not "everywhere")
- **Test:** ___ (test runner; one file per AC at `tests/use-cases/<core|craft>/UC-000/AC-#.test.*`)
- **Runtime platform:** ___

## Checked against architecture.md
<This section is required. Every line of `specs/architecture.md` this design DEPARTS FROM
belongs here with a reason and an `ADR-###`. If nothing departs, write exactly one line
"no departures" — but write it, because "silent" and "checked, matches" look identical
on disk.>

| Section in architecture.md | This design | Departs? |
|---|---|---|
| Stack | ___ | no |
| Runs where | ___ | no |
| Callers | ___ | no |
| Boundaries | ___ | no |
| Forbidden | ___ | no |

## Checked against brief
<What the source brief (`brief_path` in `.sdd/config`) asks for that this design does NOT do,
and why. No brief in the project → write "project has no source brief". This section exists
because the brief is the only thing in the repo no other check is allowed to look at.>

## Code structure
<The files and folders that will appear or change. Real paths, not a generic diagram.>

```
src/<...>
tests/use-cases/<core|craft>/UC-000/
```

## Risks and complexity
<Where it is easy to get wrong, what is being traded off. One line each. "No risks" is a
suspicious answer — it usually means nobody looked, not that there are none.>
