# UC-000 — evidence trail

<!-- The trail of UC-000: the adversarial pass, the re-read, the history. It lives HERE, beside the UC, and
     never in UC-000.md (8.0.0). Measured reason: on a real repo 56% of every byte of UC body was trail, and
     the largest UC file was 455 KB of which 72% was trail — read again by every session, of every open UC,
     forever. The UC body keeps one `## Evidence` line pointing here; the gate reads the sections below.
     Append only. Nothing here is ever rewritten: that is what makes it evidence. -->

## Adversarial pass
- Run date: ___ · Fresh session: [ ]
- Role end customer: <Q1 question [related: step N] → spec: RULE-### | → Open Question | → Out of Scope>
  (an `→ spec` output MUST carry the ID of what it created — the gate checks that ID exists)
- Role operations/accounting: ...
- Role abuser: ...

## Re-read
- Run date: ___ · Unprimed head: <subagent | fresh session>
- F1 <finding> [anchor: Main N · RULE-###] → <fix UC-### Main N | Open Question | false positive because ...>
  (`/sdd-solo:verify UC-###` writes this section. The gate wants at least ONE F# line
   carrying both `[anchor: ...]` and an output other than `___`, and the re-read commit
   must be the newest spec commit — since 6.0.0 this is the only door, there is no
   "wait overnight" any more.)

## History
- v1 (YYYY-MM-DD, owner): initial
- v2 (YYYY-MM-DD, owner): <added AC-# after the ___ role adversarial pass>
