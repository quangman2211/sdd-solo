# specs/ · docs/ · changes/ — scaffold bởi plugin sdd-solo

Folder này được tạo bởi `/sdd-solo:init`. Cập nhật bằng `/sdd-solo:init --update` — chỉ file chưa sửa tay bị ghi đè.

Bắt đầu: đọc `specs/README.md`, rồi chạy `/requirements` (AIUP) hoặc tự viết `specs/br.md`.
Mỗi buổi: mở Claude Code trong repo — hook SessionStart sẽ đọc `STATE.md` và nói anh đang ở bước nào.
