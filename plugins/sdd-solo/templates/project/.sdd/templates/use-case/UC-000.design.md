# Thiết kế: UC-000 — <tên UC>

**UC:** `specs/contexts/<ctx>/use-cases/UC-000-<slug>/UC-000.md` ← spec thật nằm ở đó, đây chỉ là *cách dựng*
**Cổng:** `.sdd/gate/UC-000.ok` · **Ngày:** ___ · **Status:** draft | agreed

## Tóm tắt
<Ba tới năm câu: UC này dựng ra cái gì, bằng đường nào. Viết cho người sáu tháng sau.>

## Bối cảnh kỹ thuật
- **Ngôn ngữ / runtime:** ___
- **Phụ thuộc chính:** ___
- **Lưu trữ:** ___ (bản GỐC của dữ liệu nằm ở đâu — không phải "chỗ nào cũng có")
- **Test:** ___ (bộ chạy test; mỗi AC một file `tests/use-cases/<ctx>/UC-000/AC-#.test.*`)
- **Nền chạy:** ___

## Đối chiếu architecture.md
<Bắt buộc có mục này. Mỗi dòng của `specs/internal/architecture.md` mà thiết kế này ĐI KHÁC
phải nằm ở đây, kèm lý do và một `ADR-###`. Không lệch chỗ nào thì viết đúng một dòng
"không lệch chỗ nào" — nhưng phải viết ra, vì "im lặng" và "đã đối chiếu, khớp" trông
giống hệt nhau trên đĩa.>

| Mục trong architecture.md | Thiết kế này | Lệch? |
|---|---|---|
| Ngăn xếp | ___ | không |
| Nơi chạy | ___ | không |
| Ai gọi | ___ | không |
| Ranh giới | ___ | không |
| Cấm | ___ | không |

## Đối chiếu brief
<Brief nguồn (`brief_path` trong `.sdd/config`) đòi gì mà thiết kế này KHÔNG làm, và vì sao.
Không có brief thì ghi "dự án không có brief nguồn". Mục này tồn tại vì brief là thứ duy
nhất trong repo mà không phép kiểm nào khác được giao nhìn tới.>

## Cấu trúc code
<Những file/thư mục sẽ mọc ra hoặc bị sửa. Đường dẫn thật, không phải sơ đồ chung chung.>

```
src/<...>
tests/use-cases/<ctx>/UC-000/
```

## Rủi ro & độ phức tạp
<Chỗ nào dễ sai, chỗ nào đang đánh đổi. Một dòng mỗi thứ. Không có rủi ro nào là một câu
trả lời đáng ngờ — thường nghĩa là chưa nghĩ tới, không phải là không có.>
